import 'package:flutter/material.dart';

class AppColors {
    static const primary = Color(0xFF4E2E1F); 
    static const secondary = Color(0xFF5B4835); 
    static const backgroundBox = Color(0xFFFDF9DF);
    static const textPrimary = Color(0xFF1C2529);
    static const textSecondary = Color(0xFF1C2529);
    static const accent = Color(0xFFFBF2C0);
    static const background = Color(0xFFFFFFFF);
}